export enum BorderRadius {
    small = '4px',
    medium = '8px',
    large = '12px',
}

export enum ZIndex {
    negative = -100,
    positive = 100,
    dropdown = 800,
    navbar = 1000,
    portal = 4000,
}
