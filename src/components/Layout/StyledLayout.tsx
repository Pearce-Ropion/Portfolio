import { StyledComponent } from '@emotion/styled';

import { StyledLayoutProps } from 'components/Layout';
import styled from 'components/styled';

export const StyledLayout: StyledComponent<StyledLayoutProps> = styled.div();
